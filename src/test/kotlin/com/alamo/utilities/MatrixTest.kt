

package com.alamo.utilities

import org.junit.Assert.assertEquals
import org.junit.Test

class MatrixTest {
  @Test fun testMyLanguage() {
    assertEquals(10, 10)
      assertEquals("Kotlin", Matrix().kotlinLanguage().name)
      assertEquals(10, Matrix().kotlinLanguage().hotness)
  }
  @Test fun otroTest() {
    assertEquals(10, 10)
      assertEquals("Kotlin", Matrix().kotlinLanguage().name)
      assertEquals(10, Matrix().kotlinLanguage().hotness)
  }
}
